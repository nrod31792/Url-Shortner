<%= form_for(@url) do |f| %>
  <div class="field">
    <%= f.label :Please_enter_full_Url_here %>
    <%= f.text_field :long_url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
